#include "holberton.h"
/**
 * _isalpha - Checks for alphabet character from a given input
 *
 * Return: Always 1 for lowercase character. 0 for the other
 * @c: The character in ASCII code
 *
 */
int _isalpha (int c)
{
  if ((c >= 97 && c <=122) || (c >= 65 && c < = 90))
	{
	  return (1);
	}
    }
  else
    {
      return (0);
    }
_putchar('\n');
}

    
